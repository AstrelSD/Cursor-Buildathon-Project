{
  "name": "cursor-buildathon-project",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "next": "16.2.6"
  }
}
